[platformio]
default_envs = uno

[env:uno]
platform = atmelavr
board = uno
framework = arduino

; Using local MCP_CAN library files (mcp_can.cpp, mcp_can.h) from src/
; No external library dependencies needed - all code is local
; Built-in Arduino libraries (SPI, EEPROM, SoftwareSerial) are automatically available

; Serial communication
monitor_speed = 115200
monitor_port = /dev/ttyACM0
upload_port = /dev/ttyACM0
; Arduino Uno (VID_2341&PID_0043) appears as /dev/ttyACM0 in WSL

; Build type for debugging (enables better stack traces)
build_type = debug
